package gerard.agente.modelousuario;

/**
 * Parte da dimensão 3 do Modelo do Usuário (Quadro 5.60: "sexo"). Combobox
 * em vez de texto livre — decisão do usuário em 2026-07-22, seguindo
 * convenção atual de telas de cadastro (inclui opção "Outro").
 */
public enum Genero {
    MASCULINO,
    FEMININO,
    OUTRO
}
