package gerard.agente.modelousuario;

/**
 * Dimensão 1 do Modelo do Usuário (Quadro 5.60): nível de complexidade da
 * tarefa em que o usuário está, dentro de uma categoria de estrutura
 * aditiva (Magina et al., 2000). Ver gerard-modelo-usuario/SKILL.md.
 */
public enum NivelComplexidadeTarefa {
    BASICO,
    INTERMEDIARIO,
    AVANCADO
}
